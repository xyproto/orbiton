on: [push, pull_request]
name: Build
env:
  GOFLAGS: -mod=vendor
jobs:
  test:
    strategy:
      matrix:
        go-version: [1.21.x]
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Install Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Update deps
      run: cd v2 && (go get -d -v; go get -t -v; go get -u -v; go mod vendor; go mod tidy; go mod vendor; go mod tidy)
    - name: Build
      run: cd v2 && go build
    - name: Test
      run: cd v2 && go test

  test-cache:
    runs-on: ubuntu-latest
    steps:
    - name: Install Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.21.x
    - name: Checkout code
      uses: actions/checkout@v4
    - uses: actions/cache@v3
      with:
        path: |
          ~/go/pkg/mod              # Module download cache
          ~/.cache/go-build         # Build cache (Linux)
          ~/Library/Caches/go-build # Build cache (Mac)
          '%LocalAppData%\go-build' # Build cache (Windows)
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-
    - name: Update deps
      run: cd v2 && (go get -d -v; go get -t -v; go get -u -v; go mod vendor; go mod tidy; go mod vendor; go mod tidy)
    - name: Build
      run: cd v2 && go build
    - name: Test
      run: cd v2 && go test
