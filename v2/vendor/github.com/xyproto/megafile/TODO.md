# Plans

- [x] Move files to the trash with the `delete` key, and undo the operations with `ctrl-z` / `ctrl-u`.
- [ ] Support deleting empty directories by being in them and pressing `delete`. Should also be undoable.
